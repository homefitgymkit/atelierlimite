{
  "name": "atelier-limite-storefront",
  "version": "1.0.0",
  "private": true,
  "description": "Atelier Limit\u00e9 storefront. JSX sources are prebuilt to app.js; no in-browser Babel.",
  "scripts": {
    "dev": "vite",
    "build": "vite build && vite build --ssr src/entry-server.jsx --outDir dist-ssr && node scripts/prerender.mjs",
    "preview": "vite preview"
  },
  "devDependencies": {
    "@vitejs/plugin-react": "^6.0.2",
    "vite": "^8.0.16"
  },
  "dependencies": {
    "react": "^18.3.1",
    "react-dom": "^18.3.1"
  },
  "type": "module"
}